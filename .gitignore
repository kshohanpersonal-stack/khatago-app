# Built outputs
*.iml
.gradle/
build/
local.properties
.idea/
.DS_Store
captures/
.externalNativeBuild/
.cxx/
# Never commit signing material or machine-local secrets
*.jks
*.keystore
*.p12
*.pem
keystore.properties
secrets.properties
# Python scratch from the static checkers in tools/
__pycache__/
*.pyc
# Local, non-production demo scratch
/scratch/
# Written by ci.yml's diagnose job inside the workspace, so the four probe logs survive to the push
# step in a red run. Never a build input, never committed.
/.diag/
